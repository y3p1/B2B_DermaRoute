  Onboarding Report: Derma Route (Demo Branch)
                                                                                                                                                           
  ---                                                                                                                                                      
  1. Project Overview                                                                                                                                      
                                                                                                                                                           
  Derma Route is a multi-role healthcare provider portal for the tissue graft industry. It manages the full lifecycle of Benefits Verification (BV)   
  requests — verifying insurance coverage before tissue procedures — along with Business Associate Agreements (BAAs), product orders, and a product
  catalog.

  Three user roles:
  - Providers — submit BV requests, sign BAA agreements
  - Admins — manage everything: verify BV requests, countersign BAAs, manage products/forms/users
  - Clinic Staff — same management access as admins

  ---
  2. Tech Stack

  ┌──────────┬───────────────────────────────────────────────────────────────────────┐
  │  Layer   │                              Technology                               │
  ├──────────┼───────────────────────────────────────────────────────────────────────┤
  │ Frontend │ Next.js 16 (App Router), React 19, TypeScript, Tailwind CSS 4         │
  ├──────────┼───────────────────────────────────────────────────────────────────────┤
  │ UI       │ shadcn/ui (Radix), TanStack Table, Lucide icons                       │
  ├──────────┼───────────────────────────────────────────────────────────────────────┤
  │ Forms    │ React Hook Form + Zod, React Signature Canvas                         │
  ├──────────┼───────────────────────────────────────────────────────────────────────┤
  │ State    │ Zustand (auth store)                                                  │
  ├──────────┼───────────────────────────────────────────────────────────────────────┤
  │ Backend  │ Next.js API routes (app/api/) — no separate Express server on staging │
  ├──────────┼───────────────────────────────────────────────────────────────────────┤
  │ Database │ PostgreSQL via Supabase, Drizzle ORM                                  │
  ├──────────┼───────────────────────────────────────────────────────────────────────┤
  │ Auth     │ Supabase Auth (phone OTP signin), Twilio Verify (signup OTP)          │
  ├──────────┼───────────────────────────────────────────────────────────────────────┤
  │ Email    │ SendGrid (transactional emails)                                       │
  ├──────────┼───────────────────────────────────────────────────────────────────────┤
  │ SMS      │ Twilio (OTP only)                                                     │
  ├──────────┼───────────────────────────────────────────────────────────────────────┤
  │ Storage  │ Supabase Storage (BV form PDFs, BAA signatures)                       │
  ├──────────┼───────────────────────────────────────────────────────────────────────┤
  │ Testing  │ Jest + custom test utils for API routes                               │
  ├──────────┼───────────────────────────────────────────────────────────────────────┤
  │ Hooks    │ Husky + lint-staged (pre-commit)                                      │
  └──────────┴───────────────────────────────────────────────────────────────────────┘

  Notable changes on Demo branch: Express→Next.js API route migration is complete. This branch adds a public demo portal (/demo), three practice-track
  provider portals (wound-care, ocular, lymphedema), and expanded admin features (analytics, audit logs, risk scoring, CMS policy updates, coverage plans).

  ---
  3. Project Structure

  integrity-tissue/
  ├── app/                          # Next.js pages + API routes
  │   ├── api/                      # 40+ API endpoints
  │   │   ├── admin-accounts/       # Admin/staff user creation
  │   │   ├── admin-signup/         # Admin signup
  │   │   ├── analytics/            # Dashboard analytics
  │   │   ├── audit-logs/           # Audit log CRUD
  │   │   ├── baa-providers/        # BAA agreement CRUD
  │   │   ├── bv/                   # BV request CRUD + verify
  │   │   ├── bv-forms/             # BV form upload/download/manage
  │   │   ├── bv-requests/          # BV request CRUD (legacy/compat)
  │   │   ├── clinic-staff-signup/  # Clinic staff signup
  │   │   ├── cms-policy-updates/   # CMS policy update feeds
  │   │   ├── coverage-plans/       # Coverage plan management
  │   │   ├── cron/                 # Scheduled job triggers
  │   │   ├── demo/                 # Demo mode helpers
  │   │   ├── healing-tracker/      # Wound healing tracker data
  │   │   ├── health/               # Health check
  │   │   ├── insurance-routing/    # Insurance routing rules
  │   │   ├── insurances/           # Insurance CRUD
  │   │   ├── lymphedema/           # Lymphedema orders/products
  │   │   ├── lymphedema-orders/    # Lymphedema order CRUD
  │   │   ├── manufacturers/        # Manufacturer CRUD
  │   │   ├── me/                   # Current user profile
  │   │   ├── ocular/               # Ocular orders/products
  │   │   ├── order-products/       # Product order CRUD
  │   │   ├── practice-tracks/      # Practice track config
  │   │   ├── products/             # Product catalog CRUD
  │   │   ├── providers/            # Provider management
  │   │   ├── provider-signup/      # Provider signup
  │   │   ├── reorder-tracking/     # Reorder tracking
  │   │   ├── risk-queue/           # Risk queue management
  │   │   ├── risk-scoring/         # Risk score calculation
  │   │   ├── send-otp/             # OTP send
  │   │   ├── signed-url/           # Signed URL generation
  │   │   ├── size-optimizer/       # Wound size optimizer
  │   │   ├── system-settings/      # System-wide settings
  │   │   ├── threshold-settings/   # Threshold config
  │   │   ├── verify-otp/           # OTP verify
  │   │   └── wound-sizes/          # Wound size options
  │   ├── auth/                     # Provider sign-in
  │   ├── demo/                     # Public demo portal (role switcher)
  │   ├── signup/                   # Provider signup (5-step wizard)
  │   ├── dashboard/                # Provider dashboard (wound-care track)
  │   ├── wound-care/               # Wound-care track portal (dashboard, orders, BAA)
  │   ├── ocular/                   # Ocular track portal (dashboard, orders)
  │   ├── medical-devices/          # Medical devices track portal
  │   ├── no-tracks/                # Fallback for providers without a track
  │   ├── admin/                    # Admin pages
  │   │   ├── signin/, signup/      # Admin auth
  │   │   ├── baa-providers/        # BAA management (list/view/edit)
  │   │   └── users/new/            # Create admin users
  │   ├── clinic-staff/             # Clinic staff pages (mirrors admin)
  │   ├── baa-providers/[id]/       # Shared BAA detail view
  │   ├── legal/                    # Legal/compliance pages
  │   └── api-documentation/        # API docs page
  │
  ├── backend/                      # Server-side logic (used by API routes)
  │   ├── controllers/              # 31 controllers
  │   ├── services/                 # 37 services (DB, SendGrid, Twilio, etc.)
  │   ├── middlewares/              # Auth, CORS, rate limiting
  │   ├── utils/                    # asyncHandler, httpError
  │   └── __tests__/                # 8 test files
  │
  ├── db/                           # Drizzle ORM schemas
  │   ├── provider.ts               # provider_acct
  │   ├── admin.ts                  # admin_acct
  │   ├── clinic-staff.ts           # clinic_staff_acct
  │   ├── bv-requests.ts            # BV requests
  │   ├── bv-forms.ts               # BV form files
  │   ├── bv-products.ts            # Order products + wound sizes
  │   ├── products.ts               # Product catalog
  │   ├── manufacturers.ts          # Manufacturers
  │   ├── insurances.ts             # Insurance types
  │   ├── baa-provider.ts           # BAA agreements
  │   ├── order-outcomes.ts         # Product order lifecycle
  │   ├── wound-measurements.ts     # Healing tracker data
  │   ├── audit-logs.ts             # Audit trail
  │   ├── coverage-plans.ts         # Coverage plan definitions
  │   ├── cms-policy-updates.ts     # CMS policy feed
  │   ├── insurance-routing.ts      # Insurance routing rules
  │   ├── lymphedema-orders.ts      # Lymphedema order records
  │   ├── lymphedema-products.ts    # Lymphedema product catalog
  │   ├── ocular-orders.ts          # Ocular order records
  │   ├── ocular-products.ts        # Ocular product catalog
  │   ├── practice-tracks.ts        # Practice track config
  │   ├── system-settings.ts        # System-wide settings
  │   ├── threshold-settings.ts     # Threshold config
  │   └── enums.ts                  # provider_role, baa_status, approver_role
  │
  ├── components/
  │   ├── admin/                    # Admin-specific components
  │   ├── clinic-staff/             # Clinic staff dashboard (7+ tabs)
  │   ├── dashboard/                # Shared: BV tables, product orders, BAA views
  │   ├── demo/                     # Demo portal components (RoleSwitcher, banners)
  │   ├── landing/                  # Landing page components
  │   ├── auth/signup/              # Signup flows (provider, admin, clinic-staff)
  │   └── ui/                       # shadcn/ui primitives
  │
  ├── supabase/rls/                 # Row Level Security SQL policies (20 files)
  ├── scripts/applyRls.ts           # Auto-apply RLS after migrations (npm run db:rls)
  ├── docs/                         # Architecture & feature docs
  ├── lib/                          # API client, auth utils, feature flags
  ├── store/                        # Zustand auth store
  └── hooks/                        # useRouteGuard

  ---
  4. What's Implemented

  Auth & Accounts

  - Phone OTP sign-in for all 3 roles (separate flows per role)
  - Provider 5-step signup wizard with BAA agreement + digital signature
  - Admin and clinic staff signup flows
  - Admin can create new admin/clinic-staff accounts
  - Conflict detection (duplicate phone/email)
  - OTP rate limiting (server + client side) with Twilio fraud guard handling

  BV Requests (Core Feature)

  - Create BV requests with clinical details (wound type/size/location, insurance, ICD-10)
  - List requests (providers see own, admins/staff see all)
  - Verify/approve requests (admin/staff)
  - Status notifications via email to providers

  BAA Management

  - Two-stage digital signature flow: provider signs → admin/staff countersigns
  - Signatures stored in Supabase Storage
  - Status workflow: signed → pending → approved
  - Email notifications on status changes

  BV Forms

  - Upload BV form PDFs per manufacturer (OX, TIDE, Tiger, etc.)
  - Filter by manufacturer, commercial type, search
  - Signed URL downloads (1hr expiry), bulk download
  - Full CRUD with storage cleanup on delete

  Product Orders

  - Create orders linked to BV requests
  - Status tracking: pending → shipped → completed / cancelled
  - Admin/staff CRUD with edit/delete

  Product Catalog

  - Manufacturers, Products, Insurances — full CRUD for each
  - Products include Q-codes, pricing, wound size associations, commercial flag
  - Quarter/year tracking for pricing periods

  Admin/Clinic Staff Dashboard

  - 7+ tabs: Product Orders, BV Requests, BAA Providers, Products, Manufacturers, Insurances, BV Forms
  - Searchable data tables with pagination and skeleton loading
  - Verification modals, confirmation dialogs, status badges

  Notifications

  - SendGrid email: new BV requests, account creation, BAA status changes
  - Professional HTML templates
  - Batch sending to all admins/staff

  Practice Tracks (Demo Branch)

  - Three provider portals: wound-care, ocular, lymphedema (each with dashboard + orders)
  - Public demo portal at /demo — role switcher, no login required
  - Demo mode: bypasses auth gates, disables inactivity auto-logout
  - practice_tracks config drives which portal a provider sees post-login
  - medical-devices track and no-tracks fallback page

  Analytics & Risk

  - Analytics dashboard (admin)
  - Risk scoring engine + risk queue management
  - Reorder tracking
  - Size optimizer (wound size recommendations)
  - Healing tracker (wound measurements over time)

  Compliance & Settings

  - CMS policy update feed
  - Coverage plan management
  - Insurance routing rules
  - Audit logs (full action trail)
  - System settings + threshold settings (admin-configurable)

  Security

  - RLS policies for all tables + storage buckets (20 SQL files)
  - Auto-apply script after db:push (scripts/applyRls.ts)
  - Role-based middleware (requireAuth, requireAdminOrClinicStaff)

  ---
  5. What's Incomplete or Concerning

  ┌──────────────────────────┬─────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┐
  │           Item           │                                                         Details                                                         │
  ├──────────────────────────┼─────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
  │ 83 console.log           │ Debugging logs scattered through production code (19 files in backend/) — needs cleanup                                 │
  │ statements               │                                                                                                                         │
  ├──────────────────────────┼─────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
  │ order_products dual      │ Table serves as both product metadata templates AND order instances — confusing schema                                  │
  │ purpose                  │                                                                                                                         │
  ├──────────────────────────┼─────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
  │ In-memory rate limiting  │ OTP abuse guard uses Map() — resets on deploy, doesn't work across serverless instances                                 │
  ├──────────────────────────┼─────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
  │ No frontend tests        │ Only backend tests exist (8 files in backend/__tests__/)                                                                │
  └──────────────────────────┴─────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┘

  ---
  6. Code Quality & Patterns

  Strengths:
  - Clean controller → service → DB layer separation
  - Consistent Zod validation on API inputs
  - Typed API client with generics
  - Comprehensive RLS policies with documentation
  - Good error handling (HttpError class, centralized error middleware)
  - Pre-commit hooks enforce lint + build

  Patterns:
  - Multi-role auth with separate middleware chains
  - Shared dashboard component (ClinicStaffDashboardClient) reused for admin with titleOverride
  - Zustand for auth state, React Hook Form for forms
  - Fire-and-forget notifications (don't block API responses)

  Debt:
  - The order_products table is overloaded — serves two conceptually different purposes
  - Some controllers are large — could benefit from further service extraction
  - No database indexes defined beyond primary keys in schema files

  ---
  7. Entry Points & User Flows

  Provider flow:
  /signup → clinic details → verify phone (OTP) → sign BAA agreement → confirm → auto-login → track portal
    - wound-care track: /wound-care/dashboard
    - ocular track: /ocular/dashboard
    - no track assigned: /no-tracks
    - legacy/default: /dashboard

  Admin flow:
  /admin/signup → verify phone → /admin dashboard → manage BV requests, BAAs, products, forms, users, analytics, risk queue

  Clinic Staff flow:
  /clinic-staff/signup → verify phone → /clinic-staff dashboard → same management as admin

  Demo portal flow (no login):
  /demo → choose role (Provider / Admin / Clinic Staff) → track-specific dashboard with mock data

  BV Request lifecycle:
  Provider creates → admins/staff notified via email → admin verifies/approves → provider notified of status

  ---
  8. Dependencies & Configuration

  Environment variables needed (.env.local):
  # Supabase
  NEXT_PUBLIC_SUPABASE_URL=https://xxx.supabase.co
  NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJ...
  SUPABASE_SERVICE_ROLE_KEY=eyJ...          # BACKEND ONLY - no NEXT_PUBLIC_

  # Twilio
  TWILIO_ACCOUNT_SID=AC...
  TWILIO_AUTH_TOKEN=...
  TWILIO_VERIFY_SERVICE_SID=VA...

  # SendGrid
  SENDGRID_API_KEY=SG...
  SENDGRID_FROM_EMAIL=noreply@integritytissue.com

  # App
  APP_URL=http://localhost:3000
  NEXT_PUBLIC_APP_NAME=Derma Route
  NEXT_PUBLIC_ALLOW_INTERNATIONAL_PHONE=false

  Setup:
  npm install
  # Create .env.local with vars above
  npm run db:push              # Push schema + apply RLS
  npm run seed:admin           # Seed admin account
  npm run seed:manufacturers:q1
  npm run seed:products:q1
  npm run seed:insurances
  npm run seed:clinic-staff
  npm run seed:provider
  npm run seed:demo            # Full demo data reset (Demo branch only)
  npm run dev                  # Dev server at :3000
  npm test                     # Run backend tests

  ---
  9. Recommended First Steps

  1. Get credentials — You need Supabase project access, Twilio account, and SendGrid API key from whoever provisioned them. Check if there's a shared .env
   or secrets manager.
  2. Get it running — npm install, set up .env.local, npm run dev. Walk through all 3 signup flows.
  3. Try the demo portal first — /demo requires no login. Use it to walk all three provider tracks (wound-care, ocular, medical-devices) and the admin view.
  4. Clean up console.logs — 83 debug statements across 19 backend files. Quick grep and remove before any production deploy.
  5. Understand the BV lifecycle end-to-end — Create a BV request as provider, then approve it as admin. This touches the core business logic.
  6. Review the Supabase dashboard — Check auth settings, storage buckets, and verify RLS policies are actually applied (20 SQL files define them but you
  need to confirm they're active).
  7. Clarify the order_products schema — Understand whether the dual-purpose design is intentional or needs refactoring.
  8. Read the docs — docs/ folder has architecture decisions, email flow diagrams, and RBAC setup notes that explain the "why" behind design choices.