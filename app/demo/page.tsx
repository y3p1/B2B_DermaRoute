import { RoleSwitcher } from "@/components/demo/RoleSwitcher";

export const metadata = {
  title: "DermaRoute Demo — Choose a role",
};

export default function DemoPage() {
  return <RoleSwitcher />;
}
